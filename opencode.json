{
  "$schema": "https://opencode.ai/config.json",
  "skills": {
    "paths": ["skills"]
  },
  "mcp": {
    "x": {
      "type": "local",
      "command": ["npx", "-y", "@realaman90/x-mcp"],
      "enabled": true,
      "env": {
        "X_BEARER_TOKEN": "${X_BEARER_TOKEN}"
      }
    }
  }
}
